package com.edc.pps.rating.controller;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

class RatingControllerIntegrationTest {

	// TODO: write an integration test with rest assured / test rest template to check if save is working
	@Test
	void saveOrUpdate() {

		// given

		// when

		// then
	}
}